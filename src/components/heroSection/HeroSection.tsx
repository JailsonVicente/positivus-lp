import Header from "./Header";
import Home from "./Home";

export default function HeroSection() {
    return(
        <section className={`
            
        `}>
            <Header/>
            <Home/>
        </section>
    )
}